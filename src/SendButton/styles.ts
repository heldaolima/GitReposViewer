import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center'
  },
  button: {
    height: 32,
    width: 120,
    padding: 5,
    backgroundColor: "#000",
    justifyContent: 'center',
    alignItems: 'center'
  },
  text: {
    color: '#fff'
  }
});