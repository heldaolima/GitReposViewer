import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        borderWidth: 1,
        borderColor: 'black',
        padding: 30,
        width: '70%',
        minHeight: '30%',
        borderRadius: 20,
        alignItems: 'center',
        justifyContent: 'center'
    }
})