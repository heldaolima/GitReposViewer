import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({ 
  flatListContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    // width: '90%',
    marginBottom: 8,
    marginTop: 8,
    
  },

  repoContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 10,
    padding: 16,
    // width: '40%',
    borderWidth: 1,
    borderRadius: 6,
    borderColor: '#30363d'
  },

  repoText: {
    fontSize: 16,
  },
});